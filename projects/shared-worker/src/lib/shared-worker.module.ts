import { NgModule } from '@angular/core';
import { SharedWorkerComponent } from './shared-worker.component';

@NgModule({
  imports: [
  ],
  declarations: [SharedWorkerComponent],
  exports: [SharedWorkerComponent]
})
export class SharedWorkerModule { }
